#!/usr/bin/env bash
# Config for qoo (Codex debug) – add dangerous bypass + web search by default
# This works with prompts/bin/agent-wrapper when invoked as `qoo`.

# Point directly at your local debug codex binary so qoo uses the same build
BINARY=/home/bhd/Documents/Projects/bhd/qox/codex-rs/target/debug/codex

# Provide the same system file as qox
SYSTEM_FLAG=--system-file
SYSTEM_FILE=agents/codex/system.md

# Add the requested flags: dangerous bypass + search
ARGS="--dangerously-bypass-approvals-and-sandbox --search"

